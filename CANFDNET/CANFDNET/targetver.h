#pragma once

// Including SDKDDKVer.h defines the highest available Windows platform.

// Modify the following defines if you have to target a platform prior to the one specified.
// Refer to MSDN for the latest info on corresponding values for different platforms.

#ifndef LINUX_BUILD
#include <SDKDDKVer.h>
#else
// Linux build - no Windows SDK needed
#endif
